function userForm(name,emailId,gitId) {
    this.name= name;
    this.emailId= emailId;
    this.gitId=gitId;
}

function userList(name,emailId,gitId){
    this.name=name;
    this.emailId=emailId;
    this.gitId=gitId;
}